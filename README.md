# 🎬 Netflix Web App (Dockerized Deployment)

This project is a **Netflix-style web application** deployed using **Docker** and **Apache Tomcat**.  
It demonstrates how to containerize a Java web application using a `Dockerfile` and `docker-compose.yml` for simple and efficient deployment.

---

## 🛠️ Tech Stack
- **Java (WAR-based web app)**
- **Apache Tomcat 9**
- **Docker & Docker Compose**
- **JDK 17**

---
🚀 Run the App

Build and start the container in one command:
docker-compose up --build
Once the container is running, open your browser and go to:
👉 http://localhost:8080

## 📸 Preview
### Home Page
![Home Screenshot](./assets/home.png)

### Sign In Page
![Sign In Screenshot](./assets/signin.png)

### Footer
![Footer Screenshot](./assets/signin.png)


💡 Key Learnings
-How to deploy a Java web app using Tomcat inside Docker
-Simplifying deployment with Docker Compose
-Managing containers with a single command
